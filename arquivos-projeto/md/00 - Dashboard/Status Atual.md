---
tipo: dashboard
status: ativo
area: memory-wiki
prioridade: alta
criado: 2026-05-24
atualizado: 2026-06-06
tags:
  - dashboard
  - status
  - memoria
---

# Status Atual

O status consolidado do projeto e o histórico detalhado de alterações por versão podem ser consultados diretamente no documento principal de status:

👉 **[[../01 - Projeto/STATUS.md|STATUS.md (Status Geral e Versões)]]**

## Resumo Executivo (v1.24.148-alpha)

- **Geral:** M0-M32 concluídos, M33 Inteligência validado em runtime local, M33 Web Push implementado com `web-push`, PWA/manifest, gerador VAPID, `prepare:web-push-env` com `--database-url`, VAPID existente, `--vapid-from-env` e `--merge-existing`, gate `validate:web-push-tooling`, evidências JSON por `--json-output`/`--evidence-dir`, validador `validate:web-push-evidence` com consistência de origem, alvo e cronologia, override explícito `--origin`, relatório `report:web-push-homologation`, finalizador `finalize:web-push-homologation` com manifesto SHA-256 `web-push-evidence-manifest.json` e verificação automática por `verify:web-push-evidence-manifest`, confirmação visual com `--finalize`, `homologate:web-push` integrado com `--validate-evidence`, `--report`, `--require-empty-evidence-dir`, `--evidence-dir-auto`, `web-push-readiness.json` e confirmação visual rastreável por `confirm:web-push-visual`/`--require-visual-confirmation`, `homologate:web-push` validado até dry-run em staging local, `inspect:web-push-subscriptions` com origem rastreável e `prove:web-push-delivery` com `--env-file`, ponte `.env.staging`/Compose para `STAGING_VAPID_*`, preflight externo com checagem live de origem publicada, script de prova real endurecido com filtros/limpeza explícita e dry-run, runbook de homologação externa, CTA de inscrição no sino de notificações do storefront e Cypress cobrindo inscrição Web Push até `/notifications/push-subscribe`, pendente apenas de registrar subscription em dispositivo/navegador com chaves VAPID reais/origem HTTPS. M11 pesáveis endurecido: sem fallback 100g sem dado real, backend/carrinho/constraint bloqueiam fracionado sem `fractionStep > 0`, DB local/staging auditados com 0 inválidos. Fluxo Solidcom -> taxonomia CMS documentado em `CMS_MANUAL.md`, com precedência EAN confirmado, fila de pendências, fallback `classification01..04` e categoria genérica em último caso. Saneamento e Reorganização do Repositório totalmente concluídos, com links operacionais da wiki validados por script.
- **Storefront:** 100% operacional; M18 adicionou H1/SEO correto na home, vitrines por intenção (`recompra`, `ofertas`, `frescos`, `feira`, `churrasco/ocasião`, `recorrentes`), substitutos em produto/carrinho, substituição configurável por item, disponibilidade/frete no carrinho, filtros/chips melhores no Mercado, CTA móvel de carrinho, barra fixa de checkout no carrinho e stepper/ação fixa no checkout. Checkout em staging usa `FulfillmentSlot` real vindo de `/delivery/slots`, agora ignorando janelas prestes a expirar; smoke visual responsivo cobre Home, Mercado/Search, Produto, Carrinho, Checkout, modal de verificacao de entrega e sino/Web Push, e as rotas secundárias `/promocoes`, `/adega`, `/receitas` e detalhe de receita em mobile, tablet e desktop. Smoke real de staging cobre `/promocoes`, `/adega`, `/receitas` e detalhe real de receita com API real; staging agora tem acervo editorial com 5 receitas publicadas, 4 categorias, 5 capas WebP próprias em `/recipes/*.webp`, copy/SEO revisados, receitas relacionadas e produtos reais vinculados via seed idempotente, protegido por `validate:staging-recipes` com checagem de `imageUrl` por slug. UI kit do storefront cobre Receitas, Mercado/Search, Produto, Carrinho, Checkout, Adega, Home, Login, Cadastro, Conta, Fallbacks, DeliveryVerificationModal, NotificationBell e Promoções com `Button`, `Badge`, `Input`, `Select`, `Checkbox`, `Radio`, `surfaceClasses`, `buttonVariants` e `cn`; controles nativos diretos em `src/components` e `src/pages` restam apenas nos wrappers `components/ui/*` e no compat layer `LoadingButton`.
- **Admin:** Interface de catálogo convertida para tabela operacional (M39 concluído com atalhos, ações em lote, edição inline e painel lateral), gestão de categorias estilo árvore Explorer (M40-M40.4), M18 com painel por função/SLA/picking mobile, M19 com tela `Contas B2B` e shadcn/ui validado em stack real. Controles nativos diretos restam apenas nos wrappers `components/ui/*`; `sistema/admin/src` esta sem `alert()`, `prompt()` e `confirm()`. API admin de receitas/categorias foi corrigida para aceitar DTOs válidos com `ValidationPipe` estrito e passou em smoke real de criação/leitura/limpeza.
- **Infra:** Docker Compose local/staging foi reativado e validado em 02/06; em 05/06 o Docker ficou indisponivel no inicio da rodada, voltou em seguida, e staging real/mobile foi revalidado. Storefront local/staging agora publica manifesto PWA e service worker com cache no-store.
- **Web Push em 06/06:** homologação externa finalizada. `.env.staging` foi atualizado para `https://jonathan.tailf56692.ts.net` via Tailscale Funnel sem rotacionar VAPID; Chrome/Cypress e Chrome/CDP registraram subscriptions reais; o pacote `sistema/artifacts/web-push-homologation/20260606T085300Z-final` fechou com readiness externo/live OK, `total=6 complete=6`, dry-run `targets=1 failed=0`, envio real `sent=1 failed=0`, confirmação visual por histórico de notificações do Windows e manifesto final com 7 artefatos verificado. `NotificationBell` foi corrigido para nao marcar permissao concedida como subscription ativa.
- **Categorias em 06/06:** `validate:category-tree` criado e executado contra staging local `http://localhost:4001` e staging HTTPS `https://jonathan.tailf56692.ts.net/api`; ambos passaram com 65 categorias CMS, 72 categorias comerciais, 45 raízes e 20 filhos. `go-live-ops.ps1 preflight` agora inclui essa validação. Produção final ainda depende de DNS/deploy, pois `antenorefilhos.com.br` resolve, mas `pedidos.antenorefilhos.com.br` falha com `getaddrinfo ENOTFOUND`.
- **Pagamento manual em 06/06:** `CARD` foi alinhado ao fluxo offline/cartao na entrega no backend, o admin reconhece aliases de PIX/Dinheiro/Cartao, e a validacao local fechou com backend focado 28/28, checkout Cypress 5/5, pedidos admin Cypress 5/5 e builds backend/admin OK.
- **Imagens de produto em 06/06:** frente operacional pausada e tooling removido. Foram excluidos scripts de importacao/auditoria/sumarizacao e artefatos `product-image-audit*`; imagens ja existentes em `uploads/products` e `uploads_staging/products` foram preservadas. Quando fotos voltarem, a solucao deve ser redesenhada com fornecedor/ERP/CMS, sem curadoria manual item a item.
- **Higiene do Repositório:** `.opencode/` e logs deletados da raiz, arquivos de taxonomia centralizados em `arquivos-projeto/`, volume mount corrigido e validado.
- **Testes:** Admin `npm run lint` OK, `npm run build` OK, Cypress admin mock 85/85 OK e Cypress admin completo em stack real 25 specs / 88 testes OK, incluindo `critical-flows.cy.ts` 3/3. M33 relatório executivo JSON/CSV respondeu 200 em runtime local. M11 pesáveis/fractionStep: frontend unit 64/64 OK, backend focado 42/42 OK, backend/frontend builds OK, migrations local/staging aplicadas, constraint Postgres confirmada, DB local 1460 fracionados/0 inválidos, DB staging 0 inválidos, Docker local/staging rebuildado/recriado e `product-pricing.cy.ts` passou 5/5 contra `3000` e 5/5 contra `4000`; Browser confirmou `/mercado` com 24 cards no local e staging. M33 Web Push focused tests 5/5 OK, gerador VAPID OK em PowerShell/env/JSON e com falha tratada para subject inválido, `prepare:web-push-env` OK com arquivo HTTPS aceito pelo preflight externo, `--database-url`, chaves VAPID fornecidas, `--vapid-from-env` com hash fonte/derivado igual e `--merge-existing` preservando `NODE_ENV`/`CUSTOM_KEEP`, `validate:web-push-tooling` OK cobrindo geração, readiness JSON, reutilização VAPID por env, merge seguro, confirmação visual com finalização, finalização de pacote, manifesto SHA-256, verificação automática/avulsa positiva do manifesto, fixture negativa de alteração pós-finalização e fixtures negativas de origem/alvo/cronologia, `validate:web-push-evidence` OK com fixture sem envio, falha esperada de `--require-send` sem envio, fixture com envio e pacote real de dry-run gerado por `homologate:web-push -- --evidence-dir`, `confirm:web-push-visual -- --finalize` OK com fixture, `validate:web-push-evidence -- --require-readiness --require-send --require-visual-confirmation` OK com fixture completa incluindo origem da inspeção, `report:web-push-homologation -- --require-readiness --require-send --require-visual-confirmation` OK com seção de preflight e confirmação visual, `finalize:web-push-homologation` OK com fixture completa, relatório, manifesto e verificação embutida, `verify:web-push-evidence-manifest` OK no pacote íntegro e falha corretamente se artefato assinado é alterado, `homologate:web-push -- --validate-evidence --report` OK ate dry-run com subscription temporaria, validação do pacote e relatório no mesmo comando, `--report` sem `--evidence-dir` falhou corretamente, `--require-empty-evidence-dir` falhou corretamente com pasta ocupada e passou com `--force-evidence-overwrite`, `--evidence-dir-auto` criou subpasta automatica, `--evidence-run-id` inválido falhou corretamente e fluxo automático completo passou ate dry-run, cleanup `remaining=0`, `homologate:web-push -- --evidence-dir` OK ate dry-run com subscription temporaria (`targets=1 failed=0`) e evidencias JSON parseadas (`total=1 complete=1 ready=true`, `dryRunTargets=1 failed=0`), `inspect:web-push-subscriptions -- --env-file` OK com fixture temporaria (`total=1 complete=1`) e `prove:web-push-delivery -- --env-file --dry-run` OK (`targets=1 failed=0`), proteção contra overwrite e subject inválido, ponte `.env.staging`/Compose validada por sintaxe PowerShell e `docker compose config --quiet`, preflight externo `validate:web-push-readiness -- --external` OK com chaves geradas, novo preflight live `validate:web-push-readiness -- --live` OK contra staging local `127.0.0.1:4000`, Cypress `web-push-subscribe.cy.ts` OK 1/1 com VAPID temporária, dry-run de prova com subscription temporária OK e cleanup confirmado, `inspect:web-push-subscriptions` OK contra staging mostrando 0 subscriptions, `prove:web-push-delivery` falhando corretamente sem subscription real registrada e `--limit 0` falhando corretamente, UI de ativação Web Push coberta no smoke visual. Wiki operacional `npm run validate:obsidian-links` OK com 42 arquivos e 0 links quebrados. `seed:staging-recipes` OK contra staging, `validate:staging-recipes` OK contra staging com `imageUrl` por slug, assets `/recipes/*.webp` 5/5 HTTP 200, Storefront auth UI kit `auth-ui-kit.cy.ts` OK local/staging 4/4, conta/fallbacks `account-fallback-ui-kit.cy.ts` OK local/staging 4/4, storefront visual responsivo primário/staging 20/20 OK, rotas secundárias visual local/staging 16/16 OK, Cypress local consolidado Search/Promoções 36/36 OK, Cypress staging Search/Promoções + smoke real 37/37 OK, rotas secundárias reais staging 4/4 OK após capas/copy finais, staging real produto/carrinho/checkout 1/1 OK em 05/06, smoke admin real de receitas OK.

- **Auditoria Top-Tier:** P0 + M01-M20 aplicados, validados e documentados; M20 fechou pipeline, E2E crítico, release, staging, backup/restore e security scan zerado em backend/frontend/admin.
- **Checkout M05:** carrinho persistido, sessao idempotente, quote server-side de estoque/preco/entrega e bloqueio inline sem `alert()`.
- **OMS M06:** pedidos por evento e item, corte/substituicao/recalculo auditaveis e admin conectado a `/admin/orders`.
- **Picking M07:** fila de separacao, operacao item a item, peso final, falta/substituicao, conferencia com justificativa, embalagem e produtividade por separador.
- **Fulfillment M08:** areas de entrega server-side, slots com capacidade/cutoff, reserva no checkout, retirada correta no ERP, rotas e tracking basico.
- **Pagamentos M09:** ledger financeiro, eventos idempotentes, refund parcial, chargeback, conciliacao gateway x pedido e guardrail para confirmacao online.
- **Integrações M10:** conectores, outbox Postgres, jobs, tentativas, retry/backoff, DLQ, replay manual e painel operacional via API.
- **API publica/Webhooks M11:** models `ApiClient`, `WebhookEndpoint`, `WebhookDelivery` e `ApiUsageLog`; prefixo `/v1`, API key com scopes/rate limit/log, webhooks assinados, retry/backoff, DLQ e replay manual.
- **Validacao M11:** `npx prisma validate`, `npx prisma generate`, `npx prisma migrate deploy`, `npx prisma migrate status`, `npm run public-api:validate-foundation`, testes backend 28 suites / 183 testes, `npm run build:all`, runtime `/v1/orders` 200, bloqueio de scope 403, revogacao 401 e stack Docker recriada/saudavel.
- **CRM/Fidelidade M12:** perfis CRM, consentimentos, segmentos automaticos, contas/ledger de pontos e cashback, campanhas com consentimento e listas de recompra.
- **Validacao M12:** migration `20260526213000_add_crm_loyalty_foundation`, `npm run crm:validate-foundation`, backend 29 suites / 188 testes, `npm run build:all` e stack Docker recriada/saudavel.
- **BI/Analytics operacional M13:** `AnalyticsEvent` padronizado por tenant/store/canal/sessao, `MetricSnapshot`, agregacoes executivas, funil, ruptura, picking, integracoes, CRM, pagamentos e drill-down por loja/categoria/produto/canal.
- **Validacao M13:** migration `20260526223000_add_bi_operational_analytics_foundation`, `npm run bi:validate-foundation`, backend 30 suites / 191 testes, `npm run build:all` e stack Docker recriada/saudavel.
- **Observabilidade/SRE M14:** request ID, correlation ID, order trace ID, logs JSON com tenant/store, metricas p95/erros, Prometheus, health ampliado, alertas SRE, status page interna e runbooks.
- **Validacao M14:** `npm run observability:validate-foundation`, backend 31 suites / 194 testes, `npm run build:all`, endpoint `/observability/metrics/prometheus` publicado e stack Docker recriada/saudavel.
- **Marketplace/Multicanal M15:** canais `SalesChannel`, mapeamento `ChannelProduct`, pedidos externos `MarketplaceOrder`, politicas de preco/estoque por canal e ingestao de pedido externo consolidada no OMS.
- **Validacao M15:** migration `20260526233000_add_marketplace_multichannel_foundation`, `npm run marketplace:validate-foundation`, backend 32 suites / 198 testes, `npm run build:all`, stack Docker recriada/saudavel e `/health` ok.
- **Personalizacao/Recomendacao M16:** modulo `RecommendationsModule`, ledger `RecommendationEvent`, recompre por cliente, complementares por cesta, substitutos por categoria/preco/disponibilidade, vitrine por segmento/margem/disponibilidade e inteligencia operacional de ruptura/campanha/conversao.
- **Validacao M16:** migration `20260527003000_add_recommendation_intelligence_foundation`, `npm run recommendations:validate-foundation`, backend 33 suites / 202 testes, `npm run build:all`, stack Docker recriada/saudavel e `/health` ok.
- **Seguranca/LGPD M17:** modulo `DataPrivacyModule`, solicitacoes `DataSubjectRequest`, pacote de consentimentos LGPD, exportacao de dados do titular, anonimizacao com bloqueio de pedido ativo, politica de retencao e auditoria de mudancas sensiveis.
- **Validacao M17:** migration `20260530010000_add_lgpd_governance_foundation`, `npm run lgpd:validate-foundation`, backend 34 suites / 206 testes, `npm run build:all`, stack Docker recriada/saudavel e `/health` ok.
- **UX/UI premium M18:** storefront conectado ao `RecommendationsModule` para vitrines de recompra, ofertas, frescos, feira, churrasco/ocasião e recorrentes; `ProductDetail` e `Cart` exibem substitutos inteligentes; carrinho permite substituição por item e mostra disponibilidade/frete; checkout mostra revisão rápida e stepper; admin recebeu painel por função, SLA visual em pedidos e picking mobile com progresso por setor, EAN/código/peso, botões grandes e formulário inline.
- **Validacao M18:** `npm run build` em `sistema/frontend` e `sistema/admin`, `npm run test:unit` no frontend (60/60), `docker compose build storefront/admin`, recreate dos containers e Browser em `http://localhost:3000`/`3002` com H1/vitrines, carrinho preenchido, checkout com stepper/revisão, painel por função e separação sem erros de console.
- **B2B/atacarejo M19:** `BusinessAccount`/`BusinessAccountUser`, preco por conta via `PriceList.businessAccountId`, limite de credito, prazo de pagamento, pedido minimo, listas corporativas, perfil de faturamento, regras recorrentes, pedido recorrente operacional, contratos fiscal/cobranca por pedido/conta, snapshot fiscal no pedido, workflow `PENDING_APPROVAL`/`APPROVED` e tela admin `Contas B2B`.
- **Validacao M19:** migrations `20260531033000_add_b2b_business_accounts_foundation` e `20260531043000_add_b2b_minimum_order_and_corporate_lists`, Prisma validate/generate/migrate, backend build/test (34 suites / 206 testes), admin/storefront builds, Docker API/admin/storefront recriado, Browser validando tela B2B, pedido pendente aprovado pela UI, lista corporativa criada/listada, pedido abaixo do minimo bloqueado, pedido recorrente aprovado e faturamento por pedido/conta gerando contratos fiscal/cobranca com erro controlado para conectores externos sem credenciais.
- **QA/release M20:** lint/build/test de backend/frontend/admin verdes, Prisma validate/migrate status verde, seed QA idempotente criado, Cypress storefront 34/34 e admin 14/14, E2E crítico fechado, release runbook criado, staging `4000/4001/4002` homologado, `release-ops.ps1 preflight` verde, backup/restore-test validado com 109 tabelas públicas restauradas. Backend atualizado para NestJS 11 e bcrypt 6; frontend/admin atualizados para Vite 7.3.3; audit moderado zerado nos três workspaces.

Para mais detalhes sobre as últimas modificações feitas nesta sessão, veja [[Histórico de Alterações]].
